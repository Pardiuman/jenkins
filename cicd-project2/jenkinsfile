pipeline{
    agent any
    // environment{

    // }
    tools{
        maven 'apache-maven-3.0.1'

    }
    stages{
        stage('quality gates check'){
            steps{
                withSonarQubeEnv('sonar-server'){
                    sh 'mvn sonar:sonar'
                }
            }

        }
        // stage(){

        // }
    }
    // post{

    // }
}